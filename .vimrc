//Jackson Dawson - Created a .vrmc file and edited the design and interface of the file. Added line numbers, created indentation, and set a new colorscheme using vim commands through the use of "escape - shift - colon - and then the command. Saved the file using the :wq command. Then created a text file using the cp command. 
syntax on
set ruler
set autoindent
set number
set ignorecase
color slate
